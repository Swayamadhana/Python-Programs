# program to caluculate prime number and print them within a range

x=int(input(''))
y=int(input(''))

def check_prime(t):
    for i in range(2,t):
        if(t%i==0):
            return False
    return True

for t in range(x,y+1):
    if(check_prime(t)): 
        print(t)
